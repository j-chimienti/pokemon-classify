import React from 'react';
import PropTypes from 'prop-types';

function Model(props) {
    return (
        <div>

        </div>
    );
}

Model.propTypes = {};
Model.defaultProps = {};

export default Model;
